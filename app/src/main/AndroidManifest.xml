<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.course.innopolis.students">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme">
        <activity android:name=".View.LoginRegistration.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Group.GroupsActivity"
            android:label="@string/group" />
        <activity android:name=".View.Student.StudentsActivity" />
        <activity android:name=".View.Student.StudentDetailsActivity" />
        <activity
            android:name=".View.LoginRegistration.RegistrationActivity"
            android:label="@string/registration" />
        <activity
            android:name=".View.Student.UpdateStudentActivity"
            android:label="" />
        <activity android:name=".View.Admin.AdminActivity" />
        <activity android:name=".View.Journal.JournalsActivity" />
        <activity android:name=".View.Lesson.LessonsActivity" />
        <activity android:name=".View.Lesson.AddLessonActivity"></activity>
    </application>

</manifest>